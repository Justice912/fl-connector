---
project: FL Connector Suno stem reconstruction
phase: consent-driven local analysis bootstrap
status: paused
last_updated: 2026-06-22T10:02:17.885Z
repository: C:\Users\HP\Vocals APP\fl-connector
branch: master
---

# Continue Here

## Current State

The FastAPI/React FL Connector now includes an Audio-to-FL Rebuild workspace for local stem reconstruction. Project storage, upload validation, the analysis worker scaffold, corrections, approval, MIDI and ZIP export, inventory matching, FL guides, and the existing FL Piano Roll payload path are implemented.

This session completed the next setup slice: a consent-driven installer can verify and install Python 3.11 and FFmpeg through `winget`, create the isolated worker environment, install pinned worker dependencies, and refuse to continue when publisher/source metadata or compatibility checks fail. The Rebuild setup bar shows the verified installation plan and requires explicit consent.

No machine-level packages were installed during development. The feature and its mocked install paths were tested only.

## Completed Work

- Baseline repository established in commit `dd1b600`.
- Reconstruction project models, secure local storage, limits, and ownership confirmation added in `cb8d823`.
- Local analysis engine, inventory scan, reconstruction compiler, guides, and export foundation added in `5996573`.
- Reconstruction setup/project/upload/analyze/correction/approval/export APIs added in `60094ee`.
- Audio-to-FL Rebuild workspace added in `328f9d6`.
- Review workflow, recommendations, guide carousel, MIDI backup, and ZIP export completed in `cbc0862`.
- Consent-driven `winget` bootstrap added in the commit containing this handoff.

## Verification

- Backend full suite: `64 passed`.
- Backend installer-focused suite: `6 passed`.
- Frontend component suite: `3 passed`.
- Frontend production build: succeeded with Vite.
- Only deprecation warnings from Starlette/FastAPI were reported; no test failures.

## Decisions And Boundaries

- Reconstruction promises a faithful editable approximation, not recovery of Suno's hidden presets, mixer values, or original project file.
- Files stay local under `.data/reconstructions`; analysis is single-concurrency and retryable.
- MIDI is used only at sufficient confidence. Low-confidence and texture/vocal material remains aligned audio.
- Installed FL sounds are preferred. Missing tools are recommendations with official and license links; they are never downloaded automatically.
- System prerequisites require explicit consent and verified `winget` package metadata.
- The existing `flp.score.addNote(...)` Piano Roll writer remains the verified FL note-write surface.
- The current guide images are instructional interface maps, not captured FL Studio screenshots.

## Remaining Work

1. Start the backend and frontend and open the Rebuild workspace.
2. Review the displayed publishers, approve the installer consent, and run the real prerequisite installation.
3. Confirm Python 3.11, FFmpeg, Basic Pitch, and all compatibility checks report ready.
4. Upload one user-owned Suno South African dance stem pack and run end-to-end reconstruction acceptance.
5. Review BPM/key, confidence, Channel Rack order, Piano Roll patterns, Playlist placement, sound matches, mix plan, and ZIP export.
6. Capture versioned FL Studio 2025 screenshots from a blank project and replace the instructional guide maps with verified screenshots and hotspots.
7. Perform the practical FL Studio import/apply walkthrough and record any corrections discovered during acceptance.

## Resume Commands

```powershell
cd 'C:\Users\HP\Vocals APP\fl-connector\backend'
.\.venv\Scripts\python -m uvicorn app.main:app --host 127.0.0.1 --port 8765
```

In a second terminal:

```powershell
cd 'C:\Users\HP\Vocals APP\fl-connector\frontend'
npm.cmd run dev -- --host 127.0.0.1 --port 5173
```

Open `http://127.0.0.1:5173/`, select **Rebuild**, and begin with the setup consent panel.

## Next Action

Start with the live consented installer acceptance. Do not upload stems until the setup endpoint reports the local worker ready. Stop for review if Basic Pitch fails the Python 3.11 compatibility gate rather than substituting another transcription engine.
